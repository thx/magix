(function(win, S, Test, EMPTY) {
    const expect = chai.expect;
    let priFun;
    let priVar;

    const testFn = () => {
        
    };

    Test.Private.service = testFn;
})(window, window.KISSY, window.Test || (window.Test = {}), '');